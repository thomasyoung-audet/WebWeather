**WebWeather**

I made a weather display app for the command line, so now might as well make a website.

Technologies:
React/Node(Express)

Start the Node server with `node server.js` in /server, then go to /client and do `npm start`

 

